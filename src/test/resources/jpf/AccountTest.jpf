# JPF configuration for Account (concrete exploration via Verify.getInt).
# Explores many concrete (balance, value) pairs; EXCEPTION CONDITIONS stays empty
# because the listener only collects symbolic path conditions.
#
# For EXCEPTION CONDITIONS (e.g. "value > this.balance"), use:
#   ./tools/run-jpf.sh AccountTestSymbolic.jpf

@include = ${config_path}/../../../../tools/jpf-bundled.properties

target = br.ufpe.cin.witup.jpf.AccountJPFDriver
listener = br.ufpe.cin.witup.jpf.ExceptionConditionListener

search.multiple_errors = true
