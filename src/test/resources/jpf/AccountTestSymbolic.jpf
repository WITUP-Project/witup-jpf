# JPF configuration for Account symbolic execution (jpf-symbc)
# Uses symbolic.method to make run(balance, value) parameters symbolic.
# ExceptionConditionListener collects path conditions when exceptions occur.
# Run with: ./tools/run-jpf.sh AccountTestSymbolic.jpf

@include = ${config_path}/../../../../tools/jpf-bundled.properties

target = br.ufpe.cin.witup.jpf.AccountJPFDriverSymbolic
listener = br.ufpe.cin.witup.jpf.ExceptionConditionListener

# Symbolic parameters for run(balance, value)
symbolic.method = br.ufpe.cin.witup.jpf.AccountJPFDriverSymbolic.run(sym#sym)

search.multiple_errors = true

# Symbolic bounds for double
symbolic.min_double = -1000.0
symbolic.max_double = 1000.0
